# =============================================================================
# KisanMitra AI Backend — Production Dependencies
# =============================================================================
# Compatible with Python 3.11–3.13
# Install: pip install -r requirements.txt
# =============================================================================

# --- Core Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.5.0
python-multipart>=0.0.9
email-validator>=2.0.0         # Required by pydantic email fields

# --- Database (Firestore via firebase-admin) ---
# motor>=3.6.0  # REMOVED: Migrated to Firestore
redis>=5.0.0   # Optional: rate limiting cache

# --- HTTP Client ---
httpx>=0.28.0

# --- Environment ---
python-dotenv>=1.0.0

# --- ML / Deep Learning ---
numpy>=1.26.0
torch>=2.3.0
torchvision>=0.18.0
transformers>=4.43.0
scikit-learn>=1.3.0

# --- Web Scraping ---
beautifulsoup4>=4.12.0
lxml>=5.3.0

# --- Authentication (Firebase Auth — backend verification) ---
# python-jose[cryptography]>=3.3.0  # REMOVED: Auth migrated to Firebase
# passlib[bcrypt]>=1.7.4            # REMOVED: Auth migrated to Firebase

# --- Image Processing ---
Pillow>=10.4.0

# --- Data Processing ---
pandas>=2.2.0

# --- Rate Limiting ---
slowapi>=0.1.9

# --- LangChain + RAG Pipeline ---
langchain>=0.3.0
langchain-google-genai>=2.0.0
langchain-community>=0.3.0
langchain-huggingface>=0.1.0   # HuggingFaceEmbeddings (standalone)
langchain-text-splitters>=0.3.0  # RecursiveCharacterTextSplitter (standalone)
langchain-chroma>=0.2.0        # Chroma vectorstore (standalone)
chromadb>=0.5.0
sentence-transformers>=3.0.0

# --- Firebase (Data Pipeline Storage) ---
firebase-admin>=6.5.0

# --- Data Ingestion Pipeline ---
PyMuPDF>=1.24.0               # PDF text extraction (fast, C-based)
pdfplumber>=0.11.0             # PDF table extraction
apscheduler>=3.10.0            # Monthly cron scheduler

# --- Logging ---
structlog>=24.4.0

# --- Testing ---
pytest>=8.3.0
pytest-asyncio>=0.23.0
